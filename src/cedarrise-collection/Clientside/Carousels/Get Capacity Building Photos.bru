meta {
  name: Get Capacity Building Photos
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/carousels/capacity-building?max=20
  body: none
  auth: inherit
}

params:query {
  max: 20
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Get Capacity Building Photos
  
  Retrieves all photo URLs from the **Capacity Building** program gallery, sourced from Cloudinary. Results are returned in descending order by upload date (most recent first), up to a maximum of 200 photos.
  
  ## Endpoint
  `GET /api/v1/carousels/capacity-building`
  
  ## Authorization
  None — this is a public endpoint.

  ## Query Parameters
  | Parameter | Type   | Required | Default     | Description / Allowed Values | Limit  |
  |-----------|--------|----------|-------------|------------------------------|--------|
  | max       | number | No       | `36`         | Page number (integer ≥ 18)  |   99   |
  
  ## Success Response
  - **Status Code**: `200 OK`
  - **Body**:
    ```json
    {
      "success": true,
      "message": "CAPACITY BUILDING photo urls found successfully",
      "data": [
        {
          "public_id": "PAT_8056_result_bbbbb",
          "url": "https://res.cloudinary.com/..."
        }
      ],
      "meta": {
        numberOfPhotos: 18,
      },
    }
    ```
  
  ## Error Responses
  None expected under normal operation. Cloudinary connectivity issues will result in a `500 Internal Server Error`.
  
  ## Notes
  - Results are cached under `cedarrise:gallery:capacity` with a 12-hour TTL (`CACHE_TTL.GALLERY`).
  - The Cloudinary source folder is `Cedarrise Initiative/CAPACITY BUILDING`.
  - Max results per request: **200**.
}
