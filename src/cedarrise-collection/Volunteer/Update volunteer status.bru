meta {
  name: Update volunteer status
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/volunteer/{{id}}/status?status=accepted
  body: none
  auth: inherit
}

params:query {
  status: accepted
}

vars:pre-request {
  baseUrl: http://localhost:3000
  id: 7d9e187f-df5f-45a2-a654-2d7181859832
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Update Volunteer Status
  
  Updates the review status of a volunteer registration record. Used to accept, reject, or reset a volunteer application to pending. On status change, all related cache entries (single record and paginated lists) are invalidated.
  
  ## Authorization
  Requires `update` permission.
  
  ## Path Parameters
  
  | Parameter | Type   | Required | Description                                |
  |-----------|--------|----------|--------------------------------------------|
  | id        | string | Yes      | UUID of the volunteer registration record  |
  
  ## Query Parameters
  
  | Parameter | Type   | Required | Default   | Description / Allowed Values                  |
  |-----------|--------|----------|-----------|-----------------------------------------------|
  | status    | string | No       | `pending` | One of: `accepted`, `rejected`, `pending`     |
  
  ## Response
  
  **`200 OK`** — Returns the updated volunteer record.
  
  ```json
  {
    "success": true,
    "message": "Volunteer status updated successfully",
    "data": {
      "id": "uuid",
      "status": "accepted | rejected | pending",
      "name": "string",
      "email": "string",
      "volunteerAreas": ["string"]
    },
    "meta": {
      "correlationId": "uuid-v4-string"
    }
  }
  ```
  
  ### Response Fields (`data`)
  
  | Field          | Type     | Description                                          |
  |----------------|----------|------------------------------------------------------|
  | id             | string   | UUID of the volunteer registration record.           |
  | status         | string   | Updated status (`accepted`, `rejected`, `pending`).  |
  | name           | string   | Volunteer's first name.                              |
  | email          | string   | Volunteer's email address.                           |
  | volunteerAreas | string[] | Areas the volunteer is interested in.                |
  
  ## Errors
  
  | Status                      | Description                                             |
  |-----------------------------|---------------------------------------------------------|
  | `400 Bad Request`           | Invalid `id` format or unknown `status` value.          |
  | `401 Unauthorized`          | Missing or invalid authentication token.                |
  | `403 Forbidden`             | User does not have `update` permission.                 |
  | `500 Internal Server Error` | Unexpected server error.                                |
}
