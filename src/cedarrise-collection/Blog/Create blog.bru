meta {
  name: Create blog
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/blog
  body: none
  auth: inherit
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}
