meta {
  name: List a user's roles
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/admin/roles/:userId
  body: none
  auth: inherit
}

params:path {
  userId: d4fa923e-dfca-4c8b-b8a5-2dec9f061204
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # Get One User's Roles
  
  Retrieves all roles assigned to a specific user.
  
  ## Authorization
  Requires `read` permission.
  
  ## Path Parameters
  | Parameter | Type   | Required | Description                        |
  |-----------|--------|----------|------------------------------------|
  | userId    | string | Yes      | UUID of the user to look up roles for |
  
  ## Validation
  - `userId` must be a valid UUID.
  
  ## Response
  Returns the list of roles assigned to the specified user.
}
