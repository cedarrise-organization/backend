meta {
  name: List all users
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/admin/users
  body: none
  auth: inherit
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  # List all users
  
  List all users
}
